pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

plugins {
    // Automatically downloads a matching JDK 21 toolchain if one is not present.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'mall-room-builder'
